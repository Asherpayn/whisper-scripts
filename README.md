# OpenAI Whisper scripts

This is a collection of my (mostly) Python scripts that make use of `openai-whisper` \
In the future this repo will start to populate with other scripts and tools in any other language.

whisper models are stored at: `~/.cache/whisper`